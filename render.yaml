# Render Blueprint - Ace MCP API Only
# Deploys ONLY the Ace MCP indexing API (not kiro2api proxy)

services:
  - type: web
    name: kiro2api-great
    runtime: python
    plan: free
    buildCommand: pip install -r acemcp-api/requirements.txt
    startCommand: cd acemcp-api && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ACE_API_TOKEN
        generateValue: true
      - key: SENTRY_DSN
        value: https://9acbe7048dee09ecb8ac5c686863d500@o4510395858616320.ingest.us.sentry.io/4510396025077760
